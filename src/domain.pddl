(define (domain planner)
  (:requirements :adl :fluents)

  (:types book month)

  (:functions

  )

  (:predicates

    (BOOK ?x)
    (MONTH ?X)
    (bookAssigned ?x)
    ;;(booksAreParallel ?x ?y)
    ;;(bookIsPredecessor ?x ?y)
	)


	(:action assignBook

	)
)
